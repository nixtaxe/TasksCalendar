<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="zabortceva.eventscalendar">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory">

        <activity android:name="zabortceva.eventscalendar.activity.AddEditTaskActivity"
            android:parentActivityName="zabortceva.eventscalendar.activity.CalendarActivity"/>
        <activity android:name="zabortceva.eventscalendar.activity.EventsActivity"
            android:parentActivityName="zabortceva.eventscalendar.activity.CalendarActivity"/>
        <activity android:name="zabortceva.eventscalendar.activity.AddEditEventActivity"
            android:parentActivityName="zabortceva.eventscalendar.activity.EventsActivity"/>
        <activity android:name="zabortceva.eventscalendar.activity.CalendarActivity"></activity>
        <activity android:name="zabortceva.eventscalendar.activity.LoginActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name="zabortceva.eventscalendar.view.AlertReceiver"/>
        <service
            android:name="zabortceva.eventscalendar.requests.MyFirebaseMessagingService"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>